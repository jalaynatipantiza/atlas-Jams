import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({

  media: {
    height: "60vh",
    width:"45vw"
  },
  box: {
    height: "30vh",
    width: "45vw",
    overflow: "auto"
  }
});

export default useStyles;